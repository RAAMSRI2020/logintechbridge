import {call,mail,linkedin,facebook,twitter,instagram} from "../assets"
import styles from '../style'

const Header = () => (
        <header className={`${styles.flexStart} `}>
          <div>
            <img src="" alt="" />
            <span></span>
          </div>
        </header>

  )


export default Header